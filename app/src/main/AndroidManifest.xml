<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aariyan.faster">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Faster">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD-t60acnwjqGPWyWTvNH1UNHT3qkwlGzM" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".Authentication.ForgetPassThirdActivity" />
        <activity android:name=".Authentication.ForgetPassSecondActivity" />
        <activity android:name=".Authentication.ForgetPassFirstActivity" />
        <activity
            android:name=".Activity.MessagingActivity"
            android:theme="@style/Theme.Chat" />
        <activity android:name=".Activity.SettingActivity" />
        <activity android:name=".Activity.ListingActivity" />
        <activity android:name=".Authentication.Business.SignUpThirdPart" />
        <activity android:name=".Authentication.Business.SignUpSecondPart" />
        <activity android:name=".Authentication.Personal.LogInPasswordActivity" />
        <activity
            android:name=".Authentication.AccountType"
            android:theme="@style/Theme.SplashScreen" />
        <activity
            android:name=".Authentication.Personal.SignUpActivity"
            android:theme="@style/Theme.SplashScreen" />
        <activity
            android:name=".Authentication.Personal.LogInActivity"
            android:theme="@style/Theme.SplashScreen" />
        <activity
            android:name=".OnBoarding.OnBoardingScreen"
            android:theme="@style/Theme.SplashScreen" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>